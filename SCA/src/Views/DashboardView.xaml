<UserControl x:Class="SCA.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    
    <Grid>
        <StackPanel>
            <TextBlock FontFamily="/Resources/Roboto/Roboto-Bold.ttf#Roboto" FontSize="40" Foreground="#002776" Margin="0,0,0,20">Gestão de Ambientes</TextBlock>
            
            <Border Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,20" BorderBrush="#E2E8F0" BorderThickness="1">
                <StackPanel>
                    <TextBlock Foreground="#64748B" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8">Pesquisar / Filtrar por Ambiente</TextBlock>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Magnify" Width="24" Height="24" VerticalAlignment="Center" Foreground="#64748B" Margin="0,0,10,0"/>
                        <ComboBox x:Name="ComboFiltroSala" Width="400" FontSize="16" Style="{StaticResource MaterialDesignOutlinedComboBox}" SelectionChanged="ComboFiltro_SelectionChanged">
                            <!-- Items will be populated in code behind -->
                        </ComboBox>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Background="White" CornerRadius="8" Padding="15,20" Margin="0,0,0,20" BorderBrush="#E2E8F0" BorderThickness="1">
                <StackPanel Orientation="Horizontal">
                    <StackPanel Orientation="Horizontal" Margin="0,0,20,0" VerticalAlignment="Center">
                        <Ellipse Width="12" Height="12" Fill="#16a34a"/>
                        <TextBlock Text="Todos disponíveis" Margin="8,0,0,0" FontSize="14" Foreground="#64748b" FontWeight="Medium"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,20,0" VerticalAlignment="Center">
                        <Ellipse Width="12" Height="12" Fill="#ea580c"/>
                        <TextBlock Text="Parcialmente ocupado" Margin="8,0,0,0" FontSize="14" Foreground="#64748b" FontWeight="Medium"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,20,0" VerticalAlignment="Center">
                        <Ellipse Width="12" Height="12" Fill="#dc2626"/>
                        <TextBlock Text="Todos emprestados" Margin="8,0,0,0" FontSize="14" Foreground="#64748b" FontWeight="Medium"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="12" Height="12" Fill="#94a3b8"/>
                        <TextBlock Text="Indisponível / Aguardando" Margin="8,0,0,0" FontSize="14" Foreground="#64748b" FontWeight="Medium"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <ScrollViewer VerticalScrollBarVisibility="Auto" Height="500">
                <ItemsControl x:Name="icAmbientes">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="220" Height="120" Margin="0,0,20,20" CornerRadius="12" Background="{Binding CorStatus}" Cursor="Hand" MouseLeftButtonUp="RoomCard_Click">
                                <Grid>
                                    <TextBlock Text="{Binding Nome}" FontSize="18" FontWeight="Bold" Foreground="White" 
                                               HorizontalAlignment="Center" VerticalAlignment="Center" 
                                               TextWrapping="Wrap" TextAlignment="Center" Margin="15"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>

        <!-- Room Items Dialog -->
        <materialDesign:DialogHost Identifier="RoomDialog" IsOpen="False">
            <materialDesign:DialogHost.DialogContent>
                <Grid Width="600" Background="White" Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock x:Name="txtDialogTitle" FontSize="20" FontWeight="Bold" Foreground="#002776" Text="Detalhes do Ambiente"/>
                        <Button HorizontalAlignment="Right" Style="{StaticResource MaterialDesignFlatButton}" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" Foreground="#64748b" Padding="0" Height="30" Width="30">
                            <materialDesign:PackIcon Kind="Close" Width="20" Height="20"/>
                        </Button>
                    </Grid>

                    <TextBlock Grid.Row="1" Foreground="#64748B" FontSize="14" Margin="0,0,0,15">Selecione os itens que deseja solicitar ou devolver.</TextBlock>

                    <Border Grid.Row="2" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" MaxHeight="300">
                        <ScrollViewer>
                            <DataGrid x:Name="dgItems" AutoGenerateColumns="False" CanUserAddRows="False" Background="White" HeadersVisibility="Column" BorderThickness="0">
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Header="Sel." Width="50">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding CanSelect}" HorizontalAlignment="Center" Style="{StaticResource MaterialDesignCheckBox}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="Ativo / Item" Binding="{Binding Descricao}" Width="*" IsReadOnly="True"/>
                                    <DataGridTemplateColumn Header="Situação Atual" Width="150">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Background="{Binding BadgeColor}" CornerRadius="12" Padding="8,4" HorizontalAlignment="Left">
                                                    <TextBlock Text="{Binding BadgeText}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </ScrollViewer>
                    </Border>

                    <Grid Grid.Row="3" Margin="0,20,0,0">
                        <Button HorizontalAlignment="Left" Style="{StaticResource MaterialDesignOutlinedButton}" Content="Cancelar Operação" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"/>
                        <Button HorizontalAlignment="Right" Style="{StaticResource MaterialDesignRaisedButton}" Background="#16A34A" BorderThickness="0" Foreground="White" Content="Processar Selecionados" Click="ProcessSelectedItems_Click"/>
                    </Grid>
                </Grid>
            </materialDesign:DialogHost.DialogContent>
        </materialDesign:DialogHost>
    </Grid>
</UserControl>
