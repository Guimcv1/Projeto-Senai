<UserControl x:Class="SCA.Views.AprovacoesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid>
        <StackPanel>
            <TextBlock FontFamily="/Resources/Roboto/Roboto-Bold.ttf#Roboto" FontSize="40" Foreground="#002776" Margin="0,0,0,20">Solicitações Pendentes</TextBlock>
            
            <Border Background="White" CornerRadius="12" Padding="24" BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Foreground="#64748B" FontSize="14" Margin="0,0,0,20">Selecione os itens desejados na tabela e aplique uma ação em lote.</TextBlock>
                    
                    <Border BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8">
                        <DataGrid x:Name="dgAprovacoes" AutoGenerateColumns="False" CanUserAddRows="False" Background="White" HeadersVisibility="Column" BorderThickness="0" RowHeight="50">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="Sel." Width="60">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" Style="{StaticResource MaterialDesignCheckBox}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Item (ID do Empréstimo)" Binding="{Binding DescricaoItems}" Width="3*" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Ambiente" Binding="{Binding Ambiente}" Width="2*" IsReadOnly="True"/>
                                <DataGridTemplateColumn Header="Solicitante" Width="2*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="#E2E8F0" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding Solicitante}" Foreground="#334155" FontSize="12"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="Ação" Width="2*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="{Binding BadgeColor}" CornerRadius="12" Padding="12,6" HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding Acao}" Foreground="White" FontSize="12" FontWeight="Bold"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                        <Button Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="#DC2626" BorderBrush="#DC2626" Content="Recusar Selecionados" Margin="0,0,15,0" Click="Recusar_Click"/>
                        <Button Style="{StaticResource MaterialDesignRaisedButton}" Background="#16A34A" BorderThickness="0" Foreground="White" Content="Aprovar Selecionados" Click="Aprovar_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
