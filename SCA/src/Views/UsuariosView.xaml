<UserControl x:Class="SCA.Views.UsuariosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid>
        <StackPanel>
            <Grid Margin="0,0,0,20">
                <TextBlock FontFamily="/Resources/Roboto/Roboto-Bold.ttf#Roboto" FontSize="40" Foreground="#002776" VerticalAlignment="Center">Gerir Utilizadores</TextBlock>
                <Button HorizontalAlignment="Right" Style="{StaticResource MaterialDesignRaisedButton}" Background="#EA580C" BorderThickness="0" Foreground="White" Height="45" Padding="20,0" Click="NovoUsuario_Click">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AccountPlus" Width="24" Height="24" Margin="0,0,10,0"/>
                        <TextBlock FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center">Novo Utilizador</TextBlock>
                    </StackPanel>
                </Button>
            </Grid>
            
            <Border Background="White" CornerRadius="12" Padding="24" BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,0,0,20">
                <StackPanel>
                    <Border BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8">
                        <DataGrid x:Name="dgUsuarios" AutoGenerateColumns="False" CanUserAddRows="False" Background="White" HeadersVisibility="Column" BorderThickness="0" RowHeight="50">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Login" Binding="{Binding Login}" Width="*" IsReadOnly="True"/>
                                <DataGridTemplateColumn Header="Perfil" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="#E2E8F0" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding Perfil}" Foreground="#334155" FontSize="12"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="Status" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="{Binding BadgeColor}" CornerRadius="12" Padding="12,6" HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding StatusText}" Foreground="White" FontSize="12" FontWeight="Bold"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="Ações" Width="100">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Editar" Foreground="#3B82F6" Width="35" Height="35" Click="Editar_Click">
                                                <materialDesign:PackIcon Kind="Pencil" />
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Dialog for New/Edit -->
        <materialDesign:DialogHost Identifier="UsuarioDialog" IsOpen="False">
            <materialDesign:DialogHost.DialogContent>
                <Grid Width="500" Background="White" Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock x:Name="txtDialogTitle" FontSize="20" FontWeight="Bold" Foreground="#002776" Text="Novo Utilizador"/>
                        <Button HorizontalAlignment="Right" Style="{StaticResource MaterialDesignFlatButton}" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" Foreground="#64748b" Padding="0" Height="30" Width="30">
                            <materialDesign:PackIcon Kind="Close" Width="20" Height="20"/>
                        </Button>
                    </Grid>

                    <StackPanel Grid.Row="1" Margin="0,0,0,20">
                        <TextBox x:Name="txtNome" materialDesign:HintAssist.Hint="Nome Completo" Style="{StaticResource MaterialDesignFloatingHintTextBox}" FontSize="16" Margin="0,0,0,20"/>
                        <TextBox x:Name="txtLogin" materialDesign:HintAssist.Hint="Login (Ex: guilherme)" Style="{StaticResource MaterialDesignFloatingHintTextBox}" FontSize="16" Margin="0,0,0,20"/>
                        <PasswordBox x:Name="txtSenha" materialDesign:HintAssist.Hint="Senha" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" FontSize="16" Margin="0,0,0,20"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <CheckBox x:Name="chkIsAdmin" Content="Administrador" Style="{StaticResource MaterialDesignCheckBox}" Margin="0,0,20,0"/>
                            <CheckBox x:Name="chkIsAtivo" Content="Ativo" IsChecked="True" Style="{StaticResource MaterialDesignCheckBox}"/>
                        </StackPanel>
                    </StackPanel>

                    <Grid Grid.Row="2">
                        <Button HorizontalAlignment="Left" Style="{StaticResource MaterialDesignOutlinedButton}" Content="Cancelar" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"/>
                        <Button HorizontalAlignment="Right" Style="{StaticResource MaterialDesignRaisedButton}" Background="#16A34A" BorderThickness="0" Foreground="White" Content="Salvar" Click="Salvar_Click"/>
                    </Grid>
                </Grid>
            </materialDesign:DialogHost.DialogContent>
        </materialDesign:DialogHost>
    </Grid>
</UserControl>
