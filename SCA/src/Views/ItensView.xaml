<UserControl x:Class="SCA.Views.ItensView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid>
        <StackPanel>
            <Grid Margin="0,0,0,20">
                <TextBlock FontFamily="/Resources/Roboto/Roboto-Bold.ttf#Roboto" FontSize="40" Foreground="#002776" VerticalAlignment="Center">Gerir Itens</TextBlock>
                <Button HorizontalAlignment="Right" Style="{StaticResource MaterialDesignRaisedButton}" Background="#EA580C" BorderThickness="0" Foreground="White" Height="45" Padding="20,0" Click="NovoItem_Click">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Plus" Width="24" Height="24" Margin="0,0,10,0"/>
                        <TextBlock FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center">Novo Item</TextBlock>
                    </StackPanel>
                </Button>
            </Grid>
            
            <Border Background="White" CornerRadius="12" Padding="24" BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,0,0,20">
                <StackPanel>
                    <Border BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8">
                        <DataGrid x:Name="dgItens" AutoGenerateColumns="False" CanUserAddRows="False" Background="White" HeadersVisibility="Column" BorderThickness="0" RowHeight="50">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Descrição" Binding="{Binding Descricao}" Width="*" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Local (Sala)" Binding="{Binding SalaNome}" Width="*" IsReadOnly="True"/>
                                <DataGridTemplateColumn Header="Estado" Width="150">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="{Binding BadgeColor}" CornerRadius="12" Padding="12,6" HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding Estado}" Foreground="White" FontSize="12" FontWeight="Bold"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="Ações" Width="150">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="Editar" Foreground="#3B82F6" Width="35" Height="35" Click="Editar_Click">
                                                    <materialDesign:PackIcon Kind="Pencil" />
                                                </Button>
                                                <!-- We can add delete if we want later -->
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Dialog for New/Edit -->
        <materialDesign:DialogHost Identifier="ItemDialog" IsOpen="False">
            <materialDesign:DialogHost.DialogContent>
                <Grid Width="500" Background="White" Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock x:Name="txtDialogTitle" FontSize="20" FontWeight="Bold" Foreground="#002776" Text="Novo Item"/>
                        <Button HorizontalAlignment="Right" Style="{StaticResource MaterialDesignFlatButton}" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" Foreground="#64748b" Padding="0" Height="30" Width="30">
                            <materialDesign:PackIcon Kind="Close" Width="20" Height="20"/>
                        </Button>
                    </Grid>

                    <StackPanel Grid.Row="1" Margin="0,0,0,20">
                        <TextBox x:Name="txtItemDescricao" materialDesign:HintAssist.Hint="Descrição do Item" Style="{StaticResource MaterialDesignFloatingHintTextBox}" FontSize="16" Margin="0,0,0,20"/>
                        
                        <ComboBox x:Name="cbSala" materialDesign:HintAssist.Hint="Vincular à Sala" Style="{StaticResource MaterialDesignFloatingHintComboBox}" FontSize="16" Margin="0,0,0,20">
                            <!-- Items populated from code-behind -->
                        </ComboBox>

                        <ComboBox x:Name="cbEstado" materialDesign:HintAssist.Hint="Estado do Item" Style="{StaticResource MaterialDesignFloatingHintComboBox}" FontSize="16" Margin="0,0,0,20">
                            <!-- Populated from code-behind -->
                        </ComboBox>
                    </StackPanel>

                    <Grid Grid.Row="2">
                        <Button HorizontalAlignment="Left" Style="{StaticResource MaterialDesignOutlinedButton}" Content="Cancelar" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"/>
                        <Button HorizontalAlignment="Right" Style="{StaticResource MaterialDesignRaisedButton}" Background="#16A34A" BorderThickness="0" Foreground="White" Content="Salvar Item" Click="Salvar_Click"/>
                    </Grid>
                </Grid>
            </materialDesign:DialogHost.DialogContent>
        </materialDesign:DialogHost>
    </Grid>
</UserControl>
