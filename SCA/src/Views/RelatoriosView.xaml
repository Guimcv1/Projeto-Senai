<UserControl x:Class="SCA.Views.RelatoriosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid>
        <StackPanel>
            <Grid Margin="0,0,0,20">
                <TextBlock FontFamily="/Resources/Roboto/Roboto-Bold.ttf#Roboto" FontSize="40" Foreground="#002776" VerticalAlignment="Center">Relatórios e Logs</TextBlock>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource MaterialDesignRaisedButton}" Background="#16A34A" BorderThickness="0" Foreground="White" Height="45" Padding="20,0" Margin="0,0,10,0" Click="ExportarExcel_Click">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FileExcel" Width="24" Height="24" Margin="0,0,10,0"/>
                            <TextBlock FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center">Exportar Excel (Empréstimos)</TextBlock>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Chart 1: Status de Locais -->
                <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="24" BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,0,10,20">
                    <StackPanel>
                        <TextBlock Foreground="#002776" FontSize="20" FontWeight="Bold" Margin="0,0,0,20">Ocupação dos Ambientes</TextBlock>
                        <lvc:PieChart Name="chartOcupacao" Height="300" LegendLocation="Right" InnerRadius="50" Margin="0,0,0,20">
                            <!-- Series populated in code-behind -->
                        </lvc:PieChart>
                    </StackPanel>
                </Border>

                <!-- Chart 2: Locais mais usados -->
                <Border Grid.Column="1" Background="White" CornerRadius="12" Padding="24" BorderBrush="#E2E8F0" BorderThickness="1" Margin="10,0,0,20">
                    <StackPanel>
                        <TextBlock Foreground="#002776" FontSize="20" FontWeight="Bold" Margin="0,0,0,20">Ambientes Mais Solicitados</TextBlock>
                        <lvc:CartesianChart Name="chartUso" Height="300" Margin="0,0,0,20">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Title="Ambientes" Labels="{Binding LabelsSalas}" />
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Title="Solicitações" MinValue="0" />
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
